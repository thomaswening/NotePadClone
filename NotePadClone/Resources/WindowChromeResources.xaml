<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:local="clr-namespace:NotePadClone"
                    xmlns:valueConverters="clr-namespace:NotePadClone.ValueConverters">

    <valueConverters:WindowStateToCornerRadiusConverter x:Key="WindowStateToCornerRadiusConverter" />

    <!-- Maximize, Restore, Minimize Buttons -->
    <Style x:Key="WindowControlButtonStyle"
           TargetType="{x:Type Button}"
           BasedOn="{StaticResource MaterialDesignFlatButton}">

        <Setter Property="Foreground"
                Value="{Binding Foreground, RelativeSource={RelativeSource Self}}" />
        
        <Setter Property="Background"
                Value="{Binding Background, RelativeSource={RelativeSource Self}}" />
        
        <Setter Property="BorderBrush"
                Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />

        <Setter Property="BorderThickness"
                Value="0" />
        
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome"
                Value="True" />
        
        <Setter Property="IsTabStop"
                Value="False" />
        
        <Setter Property="Padding"
                Value="0" />
        
        <Setter Property="Height"
                Value="32" />
        
        <Setter Property="Width"
                Value="32" />

        <Setter Property="Margin"
                Value="0 0 4 0" />

        <Setter Property="DockPanel.Dock"
                Value="Right" />
    </Style>

    <!-- Window Style -->
    <Style x:Key="MaterialDesignWindowChrome"
           TargetType="{x:Type Window}">
        
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome ResizeBorderThickness="5"
                              CaptionHeight="32"
                              GlassFrameThickness="1"
                              UseAeroCaptionButtons="False"
                              CornerRadius="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource WindowStateToCornerRadiusConverter}}" />
            </Setter.Value>
        </Setter>

        <Setter Property="Background"
                Value="{DynamicResource MaterialDesignPaper}" />
        
        <Setter Property="BorderBrush"
                Value="{DynamicResource MaterialDesignDivider}" />
        
        <Setter Property="BorderThickness"
                Value="1" />
        
        <Setter Property="Foreground"
                Value="{DynamicResource MaterialDesignBody}" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Window Header -->
                            <Grid Grid.Row="0">
                                <DockPanel>
                                    <DockPanel.Style>
                                        <Style TargetType="{x:Type DockPanel}">
                                            <Style.Triggers>
                                                
                                                <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource TemplatedParent}}"
                                                             Value="Normal">
                                                    
                                                    <Setter Property="Margin"
                                                            Value="0" />
                                                </DataTrigger>
                                                
                                                <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource TemplatedParent}}"
                                                             Value="Maximized">
                                                    
                                                    <Setter Property="Margin"
                                                            Value="5 5 5 0" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DockPanel.Style>

                                    <!-- Window Control Buttons -->
                                    <Button x:Name="PART_CloseButton"
                                            Command="{Binding Path=DataContext.CloseWindowCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                            ToolTip="Close"
                                            Margin="0"
                                            Style="{StaticResource WindowControlButtonStyle}">
                                        
                                        <materialDesign:PackIcon Kind="WindowClose" />
                                    </Button>                                    

                                    <Button x:Name="PART_MaximizeButton"
                                            Command="{Binding Path=DataContext.MaximizeWindowCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                            ToolTip="Maximize">
                                        
                                        <Button.Style>
                                            <Style TargetType="{x:Type Button}"
                                                   BasedOn="{StaticResource WindowControlButtonStyle}">
                                                
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource TemplatedParent}}"
                                                                 Value="Normal">

                                                        <Setter Property="Visibility"
                                                                Value="Visible" />
                                                    </DataTrigger>

                                                    <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource TemplatedParent}}"
                                                                 Value="Maximized">

                                                        <Setter Property="Visibility"
                                                                Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        
                                        <materialDesign:PackIcon Kind="WindowMaximize" />
                                    </Button>
                                    
                                    <Button x:Name="PART_RestoreButton"
                                            Command="{Binding Path=DataContext.RestoreWindowCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                            ToolTip="Restore">

                                        <Button.Style>
                                            <Style TargetType="{x:Type Button}"
                                                   BasedOn="{StaticResource WindowControlButtonStyle}">

                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource TemplatedParent}}"
                                                                 Value="Normal">

                                                        <Setter Property="Visibility"
                                                                Value="Collapsed" />
                                                    </DataTrigger>

                                                    <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource TemplatedParent}}"
                                                                 Value="Maximized">

                                                        <Setter Property="Visibility"
                                                                Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>

                                        <materialDesign:PackIcon Kind="WindowRestore" />
                                    </Button>
                                    
                                    <Button x:Name="PART_MinimizeButton"
                                            Command="{Binding Path=DataContext.MinimizeWindowCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                            ToolTip="Minimize"
                                            Style="{StaticResource WindowControlButtonStyle}">
                                        
                                        <materialDesign:PackIcon Kind="WindowMinimize" />
                                    </Button>

                                    <!-- Title -->
                                    <TextBlock x:Name="PART_Title"
                                               Text="{TemplateBinding Title}"
                                               Foreground="{TemplateBinding Foreground}"
                                               FontFamily="{StaticResource MaterialDesignFont}"
                                               FontWeight="Light"
                                               FontSize="12"
                                               VerticalAlignment="Center"
                                               Margin="10 0 0 0" />
                                </DockPanel>
                            </Grid>

                            <!-- Main Content -->
                            <ContentPresenter Grid.Row="1" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
